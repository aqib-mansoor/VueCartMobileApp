/**
 * Centralized Application Navigation Routes
 * 
 * Modifying route paths here will update navigation references across the entire application instantly.
 */
export const ROUTES = {
  HOME: "/home",
  LOGIN: "/login",
  REGISTER: "/register",
  CART: "/cart",
  CHECKOUT: "/checkout",
  ORDERS: "/orders",
  PROFILE: "/profile",
  FAVORITES: "/favorites",
} as const;
